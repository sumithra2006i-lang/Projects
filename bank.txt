#include <stdio.h>
#include <stdlib.h>
#include <time.h>

void Check_Balance(int balance) {
       printf("\nbalance: %d", balance);
}

void Deposit(int *balance) {
    int amount;
    printf("Enter amount: ");
    scanf("%d", &amount);
    printf("\n*Deposit Successful*\n..............................");
    *balance += amount;
}

void Withdraw(int *balance) {
     int amount;
    printf("Enter amount: ");
    scanf("%d", &amount);
    printf("\n *Withdraw Successful*\n.............................");
    *balance -= amount;
}

void Transfer(int otp, int *balance) {
    int n, o, amount;
    srand(time(NULL));
    printf("Enter Receiver's account number: ");
    scanf("%d", &n);
    printf("OTP: %d", otp);
    printf("\nEnter OTP: ");
    scanf("%d", &o);
    if(o == otp) {
        printf("enter amount: ");
        scanf("%d", &amount);
        printf("*Transfer successful*\n.............................");
    }
    else {
        printf("Invalid OTP!!");
    }
    *balance -= amount;
}


int main()
{
    char name[20];
    int num;
    int ch;
    int otp;
    int pin, account_no;
    int initial_deposit = 0, balance = 0;
    otp = rand() % 1000 + 1000;
   // int amount[100];
    srand(time(NULL));
    printf("**********welcome to SRL bank**********\n");
    printf("Create your account : \n---------------------\n");
    printf("Enter name :");
    scanf("%s", &name);
   //pin = rand() % 10000 + 10000;
    printf("set pin:");
    scanf("%d", &pin);
    printf("Enter your initial deposit:");
    scanf("%d", &initial_deposit);
    printf("Account Created Successfully.!");
    account_no = rand() % 100000000 + 100000000;
    printf("\nyour Account number: %d", account_no);
    balance = initial_deposit;
    printf("\nLogin to your Account:\n-----------------------");
    printf("\nenter your account number: ");
    scanf("%d", &num);
    if(num == account_no) {
        printf("Login Successfull.!");
    }
    else {
        printf("Invalid Account number");
    }
   
    printf(":\nLogin Menu:\n***********");
    do{
    printf("\n1.Check Balance\n2.Deposit\n3.Withdraw\n4.Transfer\n5.mini statement\n6.Change Pin\n7.Account Details\n8.exit");
    printf("\nEnter your option:");
    scanf("%d", &ch);
    switch(ch) {
        case 1:
          Check_Balance(balance);
          break;
        case 2:
          Deposit(&balance);
          break;
        case 3:
          Withdraw(&balance);
          break;
        case 4:
          Transfer(otp, &balance);
        case 5: printf("5. Change Pin\n");
        case 6: printf("6. Account Details\n");
        case 7: printf("7. exit");
        default:printf("Invalid option");
        
    }
    }
    while(ch != 7);

    return 0;
   
}